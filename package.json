{
  "name": "budgetr",
  "private": true,
  "license": "ISC",
  "version": "0.0.1",
  "description": "Awesome project developed with TypeORM.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "nf start",
    "start:web": "lerna run start --stream --scope={@budgetr/shared,@budgetr/web}",
    "start:server": "lerna run start --stream --scope={@budgetr/shared,@budgetr/server}",
    "sync:types": "lerna run sync:types && yarn install"
  },
  "devDependencies": {
    "lerna": "^3.0.6"
  },
  "dependencies": {
    "foreman": "^3.0.1",
    "npm-run-all": "^4.1.3"
  }
}
